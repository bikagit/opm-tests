% Tables and summary for:
% "Data-Driven Modeling of Hysteresis in Porous Media:
%  Neural Network Surrogates Coupled with the OPM Flow Reservoir Simulator"
%
% All measured values obtained on a MacBook (Apple Silicon, CPU-only).
% Training uses Adam (lr=0.01), MSE loss, early stopping (patience 20/50),
% batch size 32, up to 1000 epochs, augmented dataset ~30x original size.

% ============================================================
% TABLE 1 — Single-case surrogate accuracy (Case A, Swi=0.06)
% ============================================================

\begin{table}[h]
\centering
\caption{Single-case accuracy on held-out test data: consolidated performance
metrics for the Killough-, Carlson-, and LS-LBM-trained neural network models
(Case~A, $S_{wi}=0.06$).}
\label{tab:accuracy}
\begin{tabular}{lccccc}
\hline
\textbf{Model} & \textbf{RMSE} & \textbf{MAE} & \textbf{R$^2$ Score}
  & \textbf{EndP-Err (Start)} & \textbf{EndP-Err (End)} \\
\hline
Killough NN & 0.0105 & 0.0075 & 0.9987 & 0.0076 & 0.0096 \\
Carlson NN  & 0.0116 & 0.0087 & 0.9980 & 0.0200 & 0.0005 \\
LS-LBM NN  & 0.0459 & 0.0296 & 0.9660 & 0.0100 & 0.0142 \\
\hline
\end{tabular}
\end{table}

% ============================================================
% TABLE 2 — Computational performance (Case A, Swi=0.06, CPU)
% ============================================================
%
% Training time: wall-clock time for full training run (augmented dataset,
%   early stopping, single CPU core).
% Inference / call: mean cost of one non-wetting relperm evaluation by the
%   surrogate (one "call" = one saturation point = one grid cell), measured by
%   a dedicated timer in OPM Flow with the vectorised (batched) path of
%   NNModel::applyBatch. The batch evaluates all grid cells per nonlinear
%   iteration; the per-call figure is the batch time divided by the number of
%   cells. (Per-cell cost is flat across batch sizes, so this is the genuine
%   marginal per-evaluation cost, not just an amortisation artefact.)
% Reference eval. / call: mean cost of one evaluation of the classical
%   hysteresis model (Killough/Carlson scanning-curve construction), measured
%   with the standalone reference binary, per saturation point, same hardware.
%   NB: this standalone cost includes process-launch and deck-parse overhead
%   amortised over the points in each invocation, so it overstates the pure
%   in-simulator scanning-curve arithmetic (which is sub-microsecond).

\begin{table}[h]
\centering
\caption{Computational timing metrics: offline training cost and
simulation-time inference cost with the vectorised (batched) surrogate
evaluation, compared with the classical hysteresis model evaluation cost
(Case~A, $S_{wi}=0.06$, CPU-only, MacBook). One ``call'' is a single
non-wetting relperm evaluation (one saturation point). The surrogate
inference is the vectorised, single-precision, derivative-free cost amortised
per evaluation over the batched call; the reference evaluation is the
classical hysteresis-model (Killough/Carlson scanning-curve) construction
measured with the standalone reference binary on the same hardware.}
\label{tab:timing}
\begin{tabular}{lccc}
\hline
\textbf{Model} & \textbf{Training time (CPU)} & \textbf{Inference / call}
  & \textbf{Reference eval.\ / call} \\
\hline
ML-Killough & 289.2\,s & 0.0016\,ms & 0.393\,ms \\
ML-Carlson  & 286.0\,s & 0.0016\,ms & 0.250\,ms \\
ML-LS-LBM  & 308.8\,s & 0.0016\,ms & 1.190\,ms \\
\hline
\end{tabular}
\end{table}

% ============================================================
% TABLE 2b — Training cost vs total simulation wall time
% ============================================================
%
% Same layout as Table 2, but the per-call timing columns are replaced by the
% total simulation wall-clock time (Wall) taken from Table~\ref{tab:timing_metrics}
% for Case A, Swi=0.06 (vectorised batched inference). Provides a direct
% "offline cost vs in-simulation cost" view per surrogate.

\begin{table}[h]
\centering
\caption{Offline training cost and total simulation wall-clock time per
model (Case~A, $S_{wi}=0.06$, CPU-only, MacBook). Training time applies to
the ML surrogates only; wall times are taken from Table~\ref{tab:timing_metrics}
(vectorised batched inference). The classical and no-hysteresis runs are shown
for reference.}
\label{tab:training_wall}
\begin{tabular}{lcc}
\hline
\textbf{Model} & \textbf{Training time (CPU)} & \textbf{Wall (s)} \\
\hline
No-hysteresis    & --       & 0.539 \\
Classic-Killough & --       & 0.581 \\
Classic-Carlson  & --       & 0.515 \\
ML-Killough      & 289.2\,s & 0.806 \\
ML-Carlson       & 286.0\,s & 0.772 \\
ML-LS-LBM       & 308.8\,s & 0.836 \\
\hline
\end{tabular}
\end{table}

% ============================================================
% TABLE 3 — Breakthrough time and recovery factor (multi-case)
% ============================================================
%
% Breakthrough time (BT): first day at which oil is produced at the
%   production well (OP01/OP02).
% Recovery factor (RF): cumulative oil produced / OOIP at final
%   simulation time (9 days).
% OOIP: Case A/test006 = 2124.4 mL (Swi=0.06);
%        Case A/test020 = 1808.0 mL (Swi=0.20);
%        Case B/test006 = oil injected scenario;
%        Case B/test020 = oil injected scenario.
% X in Table 4 denotes solver non-convergence (Killough: Swi >= 0.20;
%   Carlson: Swi >= 0.35).

\begin{table}[h]
\centering
\caption{Multi-case performance metrics: water breakthrough time and
oil recovery factor at final simulation time (9\,days) for the
no-hysteresis baseline and each ML surrogate across cases and initial
saturations.}
\label{tab:multicase}
\begin{tabular}{llcc}
\hline
\textbf{Case} & \textbf{Model} & \textbf{BT (days)} & \textbf{RF (\%)} \\
\hline
\multirow{4}{*}{A, $S_{wi}=0.06$}
  & No-hysteresis & 2.0052 & 26.1 \\
  & ML-Killough   & 2.0104 & 26.9 \\
  & ML-Carlson    & 2.0060 & 26.3 \\
  & ML-LS-LBM    & 2.0582 & 11.7 \\
\hline
\multirow{3}{*}{A, $S_{wi}=0.20$}
  & No-hysteresis & 0.0359 & 29.2 \\
  & ML-Carlson    & 2.0165 & 32.2 \\
  & ML-LS-LBM    & 2.0011 & 37.3 \\
\hline
\multirow{4}{*}{B, $S_{wi}=0.06$}
  & No-hysteresis & 0.0359 &  2.4 \\
  & ML-Killough   & 0.1089 &  1.5 \\
  & ML-Carlson    & 0.0359 &  1.9 \\
  & ML-LS-LBM    & 0.0359 &  1.4 \\
\hline
\multirow{3}{*}{B, $S_{wi}=0.20$}
  & No-hysteresis & 0.0359 &  2.4 \\
  & ML-Carlson    & 0.0359 &  1.3 \\
  & ML-LS-LBM    & 0.0359 &  1.2 \\
\hline
\end{tabular}
\end{table}

% ============================================================
% TABLE 4 — Simulation CPU-time comparison (all cases)
% ============================================================
%
% Wall   = sum of solver-phase columns (Assembly+LSetup+LSolve+LocSol+Update+Output)
% Asm    = assembly time (includes the once-per-iteration batched ML inference,
%          which runs in beginIteration(); see ML-inference note below)
% LSet   = linear system setup time
% LSolv  = linear solve time
% Upd    = solution-update time
% NI     = total Newton iterations
% Speedup = No-hysteresis wall / run wall for each case block
% ML inf. = dedicated-timer total cost of the batched relperm inference
%
% Vectorised (batched) inference: NNModel::applyBatch evaluates all cells in a
% single call per nonlinear iteration. Requires \usepackage{multirow}.

\begin{table}[h!]
\centering
\resizebox{\textwidth}{!}{
\begin{tabular}{|l|c|c|c|c|c|c|c|c|c|}
\hline
\textbf{Case} & \textbf{Model} & \textbf{Wall (s)} & \textbf{Asm (s)} & \textbf{LSet (s)} & \textbf{LSolv (s)} & \textbf{Upd (s)} & \textbf{ML inf.\ (s)} & \textbf{NI} & \textbf{Speedup} \\
\hline
\multirow{6}{*}{A, $S_{wi}=0.06$}
  & No-hysteresis    & 0.539 & 0.146 & 0.118 & 0.157 & 0.117 & --    &  6176 & $1.00\times$ \\
  & Classic-Killough & 0.581 & 0.154 & 0.129 & 0.168 & 0.131 & --    &  6339 & $0.93\times$ \\
  & Classic-Carlson  & 0.515 & 0.136 & 0.115 & 0.149 & 0.115 & --    &  5964 & $1.05\times$ \\
  & ML-Killough      & 0.806 & 0.316 & 0.148 & 0.193 & 0.149 & 0.134 &  7482 & $0.67\times$ \\
  & ML-Carlson       & 0.772 & 0.307 & 0.141 & 0.183 & 0.141 & 0.129 &  7206 & $0.70\times$ \\
  & ML-LS-LBM        & 0.836 & 0.329 & 0.147 & 0.197 & 0.162 & 0.141 &  7909 & $0.64\times$ \\
\hline
\multirow{4}{*}{A, $S_{wi}=0.20$}
  & No-hysteresis    & 0.798 & 0.227 & 0.171 & 0.225 & 0.175 & --    & 10181 & $1.00\times$ \\
  & Classic-Carlson  & 0.648 & 0.181 & 0.143 & 0.186 & 0.139 & --    &  8066 & $1.23\times$ \\
  & ML-Carlson       & 0.948 & 0.378 & 0.169 & 0.218 & 0.184 & 0.163 &  9219 & $0.84\times$ \\
  & ML-LS-LBM        & 0.963 & 0.391 & 0.168 & 0.222 & 0.182 & 0.168 &  9554 & $0.83\times$ \\
\hline
\multirow{6}{*}{B, $S_{wi}=0.06$}
  & No-hysteresis    & 1.019 & 0.286 & 0.218 & 0.285 & 0.231 & --    & 12151 & $1.00\times$ \\
  & Classic-Killough & 0.891 & 0.242 & 0.194 & 0.248 & 0.207 & --    & 10520 & $1.14\times$ \\
  & Classic-Carlson  & 0.861 & 0.239 & 0.182 & 0.238 & 0.202 & --    & 10215 & $1.18\times$ \\
  & ML-Killough      & 1.223 & 0.494 & 0.214 & 0.282 & 0.234 & 0.210 & 11958 & $0.83\times$ \\
  & ML-Carlson       & 1.324 & 0.529 & 0.234 & 0.310 & 0.251 & 0.226 & 12935 & $0.77\times$ \\
  & ML-LS-LBM        & 1.385 & 0.545 & 0.247 & 0.326 & 0.266 & 0.230 & 12958 & $0.74\times$ \\
\hline
\multirow{4}{*}{B, $S_{wi}=0.20$}
  & No-hysteresis    & 1.029 & 0.287 & 0.219 & 0.287 & 0.235 & --    & 12151 & $1.00\times$ \\
  & Classic-Carlson  & 0.920 & 0.258 & 0.198 & 0.257 & 0.208 & --    & 11294 & $1.12\times$ \\
  & ML-Carlson       & 1.276 & 0.506 & 0.228 & 0.300 & 0.242 & 0.216 & 12324 & $0.81\times$ \\
  & ML-LS-LBM        & 1.240 & 0.497 & 0.219 & 0.290 & 0.234 & 0.212 & 12070 & $0.83\times$ \\
\hline
\end{tabular}
}
\caption{Simulation CPU-time comparison across all cases, using the vectorised
(batched) ML relperm inference. Wall\,=\,total wall-clock time (sum of solver
phases); Asm\,=\,assembly; LSet\,=\,linear system setup; LSolv\,=\,linear solve;
Upd\,=\,solution update; NI\,=\,total Newton iterations; Speedup is relative to
the no-hysteresis baseline for each case. The batched surrogate is evaluated
once per nonlinear iteration in \texttt{beginIteration()}, so its cost is
contained in the Assembly column; the dedicated ML-inference timer (ML inf.)
reports it separately, showing it is now a small fraction ($\sim$17--20\,\%) of
the ML run wall time rather than the dominant cost. With batched inference the
ML surrogates run at $0.64$--$0.84\times$ the no-hysteresis wall time (vs.\
$\sim$$0.15\times$ for the per-cell evaluation), i.e.\ within $\sim$20--35\,\%
of a non-hysteretic simulation. All values measured on a MacBook (Apple
Silicon, CPU-only); ML inference is single-precision and derivative-free.}
\label{tab:timing_metrics}
\end{table}

% ============================================================
% SUMMARY PARAGRAPH (for Section 4.2.2 / 4.3.1)
% ============================================================

\subsubsection*{Summary of computational performance (Table~\ref{tab:timing})}

Training is performed once offline and requires approximately 5--6 minutes
per surrogate on a single CPU core (MacBook, Apple Silicon), with all three
models converging within 300--550 epochs via early stopping.
At simulation time the surrogate is evaluated through the compiled C++
inference path (Kerasify format, loaded directly by OPM Flow) using a
vectorised, single-precision, derivative-free kernel that processes all grid
cells in one call per nonlinear iteration. The resulting cost is
approximately $1.6\,\mu$s per saturation-point evaluation (one grid cell),
more than two orders of magnitude \emph{below} a single classical
scanning-curve evaluation ($0.25$--$1.2$\,ms per saturation point).
Vectorising the evaluation (versus the per-cell call of the reference
pointwise implementation) reduces the total in-simulation inference cost by
about a factor of 25 for the present cases and is the key enabler for the
overall wall-time speedups reported in Table~\ref{tab:timing_metrics}.
The reference evaluation times reported are per-saturation-point costs
of the corresponding classical hysteresis binary measured on the same
hardware and compiled configuration.

\subsubsection*{Summary of multi-case results (Table~\ref{tab:multicase})}

For Case~A ($S_{wi}=0.06$), all three ML surrogates reproduce breakthrough
times within 3\,\% of each other (2.0052--2.0582 days vs.\ 2.0052 days
for no-hysteresis), and recovery factors between 26--27\,\%, consistent
with the no-hysteresis baseline.
The exception is ML-LS-LBM, which yields a substantially lower recovery
factor of 11.7\,\%, reflecting stronger oil trapping encoded in the
pore-scale LS-LBM training data relative to the classical models.

For Case~A ($S_{wi}=0.20$), hysteresis significantly delays breakthrough
(from 0.036 to $\approx$2.0 days) and increases recovery, with
ML-LS-LBM reaching 37.3\,\% vs.\ 29.2\,\% for no-hysteresis, highlighting
the sensitivity of recovery predictions to the choice of hysteresis
representation.

For Case~B (alternating injection, $S_{wi}=0.06$ and 0.20), the
no-hysteresis baseline shows early breakthrough (0.036 days) and low
recovery ($\leq$2.4\,\%), consistent with the absence of oil trapping.
All ML surrogates produce similar results in this scenario, with
ML-Killough showing a slightly delayed breakthrough (0.109 days) for
$S_{wi}=0.06$ reflecting differences in training data between the
Killough and Carlson/LS-LBM datasets under the Scenario~B saturation
paths.
